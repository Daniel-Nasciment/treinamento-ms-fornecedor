# BOOTSTRAP E UM ARQUIVO QUE VAI SER LIDO EM UM CONTEXTO ANTERIOR AO CONTEXTO DA NOSSA APLICACAO
# ISSO FOI FEITO PQ LOGO DE CARA O HIBERNATE JA BUSCA AS INFORMACOES DO DATABASE
spring:
  application:
    name: fornecedor
  profiles:
    active: default
  cloud:
    config:
      uri: http://localhost:8888